function setup() { // setup function
createCanvas(500, 500); // creates the canvas
  background(230, 0, 0); // sets background colour red
   noStroke(); // removes outlines 
}
function draw() {  // draw function
  fill(0, 0, 230); // set colour blue
  ellipse(250, 250, 450, 450); // create circle

  fill(230, 230, 0); // set colour yellow
  ellipse(250, 250,338, 338); // create circle

  fill(0, 0, 230); // set colour blue
  ellipse(250, 250, 226, 226); // create circle

  fill(230, 230, 0); // set colour yellow
  ellipse(250, 250, 114, 114); // create circle

  fill(0, 0, 230); // set colour blue
  ellipse(250, 250, 50, 50); // create circle

}